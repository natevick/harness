Create a file called `SMOKE.txt` in the repository root containing exactly the
word `ok`, then run `python -m unittest discover -s tests -t .` once to confirm
the existing suite still passes. Report the test count and stop. Do not change
anything else and do not create any git commits.
